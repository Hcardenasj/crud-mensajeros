<div class="container">
    <div class="mt-5">
        {{#if mensajeros}}

        <table class="table text-center">
            <thead>
                <tr class="table-dark">
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Identificacion</th>
                    <th>Telefono</th>
                    <th>Edad</th>
                    <th>Municipio</th>
                    <th>Ganancia (70%)</th>
                    <th>Acciones</th>
                </tr>

            </thead>
            <tbody>
                {{#each mensajeros}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{nombre}}</td>
                    <td>{{identificacion}}</td>
                    <td>{{telefono}}</td>
                    <td>{{edad}}</td>
                    <td>{{municipio}}</td>
                    <td>{{ganancia}}</td>
                    <td>
                        <div class="btn-group">
                            <a href="/delete/{{id}}" class="btn btn-danger">
                                <i class="fa-solid fa-trash"></i></a>
                            <a href="/edit/{{id}}" class="btn btn-warning">
                                <i class="fa-solid fa-pencil"></i></a>
                            <a href="/addServ/{{id}}" class="btn btn-info" >
                                <i class="fa-solid fa-plus"></i>
                            </a>
                            <a href="/listServ/{{id}}" class="btn btn-primary" >
                                <i class="fa-solid fa-eye"></i>
                            </a>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
            <tfoot>
                <tr class="table-dark">
                    <td colspan="8">REGISTRO DE MENSAJEROS</td>
                </tr>
            </tfoot>
        </table>
    </div>
    {{else }}
    <div class="card text-center mx-auto" style="width: 350px;">
        <div class="card-header">
            <h3>Crea tu mensajero</h3>
        </div>
        <div class="card-body">
            <p>Aún no tienes creado ningún mensajero, presiona este boton para añadir.</p>
            <a href="/add" class="btn btn-primary">Ir a crear</a>
        </div>
        {{/if}}
    </div>
</div>

